# Pinned Python dependencies for the mMIMO_sleep RunPod image.
#
# NOTE: torch is intentionally NOT listed here. It is installed separately
# in Dockerfile.runpod from the official PyTorch CUDA 12.6 wheel index
# *before* this file is installed, so that its dependency resolution never
# has a chance to consider the default-PyPI CUDA 13.0 build. See
# docker/constraints.txt and docker/README.md for details.
#
# Versions below match the known-good environment recorded in
# /workspace/mMIMO_sleep/handoff20260818.md (Section 1) and are compatible
# with the version ranges declared in /workspace/mMIMO_sleep/pyproject.toml.

# --- Core scientific stack (project runtime deps, excluding torch) ---
numpy==1.26.4
matplotlib==3.11.1

# --- Sionna RT / Mitsuba / Dr.Jit ray-tracing stack (project "rt" extra) ---
sionna-rt==1.2.2
mitsuba==3.8.0
drjit==1.3.1

# --- Jupyter / notebook kernel support ---
ipykernel==7.3.0
jupyterlab==4.6.3

# --- Dev / test tooling (project "dev" extra) ---
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.3
