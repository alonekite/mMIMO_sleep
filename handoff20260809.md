# Handoff 2026-08-09

## 背景
围绕 `Projects/TAP_TCB_Resource/Ginza_012/cfr_mamimo.ipynb` 展开的讨论与环境搭建工作。

## 会话内容摘要

### 1. Notebook 内容解读
`cfr_mamimo.ipynb` 是基于 **Sionna RT（射线追踪）** 的大规模 MIMO 信道频率响应（CFR）仿真脚本：
- 场景：银座（Ginza）街区 3D 场景（`ginza_1.xml`），3.5GHz 频段。
- BS（TX）：位于 `(-122, -108.5, 41)`，`tr38901` 交叉极化天线，**4×8 阵列**，发射功率 **51.13 dBm ≈ 129.72 W**，略微下倾（俯仰约 7.5°）。
- RX（UE）：全向天线，沿距离环（10m~810m）随机/预生成分布，本次从 `rx_1000.pkl` 加载 1000 个 UE。
- 核心流程（Cell 7 / Phase 1）：分批做射线追踪 → 得到 CIR（`a`, `tau`） → 按功率阈值（30dB）过滤弱径（约 5 万条路径裁剪到 500 条）→ 转换为 OFDM 频域 CFR → 保存为 `cfr_mamimo_012.pkl`，供后续预编码/容量分析（MRT/ZF/RZF/SLNR 等，定义在 `Lib_xhd/tools/sionna_xhd/channel.py`）使用。

### 2. TX 配置问答
确认 TX 使用 `AntennaParams("s", True, False, "tr38901", "cross")` + `ArrayParams(False, 4, 8, 0.5, 0.5)`，即 3GPP TR38.901 天线方向图、交叉极化、4行×8列阵列（0.5λ 间距）。

### 3. 在 workspace 下安装 Sionna RT —— 进行中，未完成

#### 已确认的环境信息
- 系统：macOS 12.7.6 (Monterey), x86_64
- 系统自带 Python：`/usr/bin/python3` (3.9.6)
- 初始状态：无 conda、无 Homebrew、无独立 LLVM

#### 已执行的操作（按时间顺序）
1. 在 `/Users/alonekite/CascadeProjects/Study_Sionna_BX` 下用系统 Python 3.9 创建了 `.venv`（**后续将被删除重建**）。
2. `pip install sionna` 首次失败：`cryptography` 依赖需要从源码编译（缺 Rust/OpenSSL/pkg-config）。
   - 原因：pip 解析到的 `cryptography` 新版本在 macOS x86_64 + py3.9 组合下没有预编译 wheel。
3. 通过固定 `cryptography==41.0.7`（有对应 wheel）绕过编译问题。
4. 重新 `pip install sionna` 成功，但**版本解析为旧版 `sionna==0.19.2`**（而不是含 `ITURadioMaterial` 等新 API 的 1.x 版本）。
   - **根本原因**：新版 `sionna-rt`（1.x）依赖 `scipy>=1.14.1`，而 **scipy 1.14+ 不再提供 Python 3.9 的 wheel**，导致 pip 解析器一路回退到不依赖新 scipy 的旧版 `sionna`。
5. 验证 `import sionna.rt` 报错：缺少 `libLLVM.dylib`（sionna.rt 底层 mitsuba/drjit 在无 NVIDIA GPU 时用 LLVM JIT 后端，需要系统级 LLVM 共享库）。
6. 征得用户同意后，安装了 **Homebrew**（系统级，非 workspace 局限）。
7. `brew install llvm` 成功，获得 `libLLVM.dylib`：
   ```
   /usr/local/Cellar/llvm/22.1.8/lib/libLLVM.dylib
   ```
8. 用 `DRJIT_LIBLLVM_PATH` 指向该文件后，`sionna.rt` 的 LLVM 后端问题解决，但因为装的是旧版 `sionna==0.19.2`，`from sionna.rt import ITURadioMaterial` 仍然失败（旧版没有这个 API，笔记本代码依赖新版 API）。
9. 定位根因：Python 3.9 与新版 sionna-rt 依赖（scipy>=1.14.1）不兼容。
10. `brew install python@3.11` 成功，得到：
    ```
    /usr/local/bin/python3.11  (Python 3.11.15)
    ```
    （过程中有一个 `post-install` 步骤的非致命警告，可执行文件已确认可用）

#### 尚未完成（下次继续）
1. 删除旧的 `.venv`（基于 Python 3.9.6）：
   ```bash
   rm -rf /Users/alonekite/CascadeProjects/Study_Sionna_BX/.venv
   ```
2. 用 Python 3.11 重新创建虚拟环境：
   ```bash
   /usr/local/bin/python3.11 -m venv .venv
   .venv/bin/pip install --upgrade pip
   ```
3. 安装新版 `sionna`（应能直接解析到 1.x 系列，含 `ITURadioMaterial` 等 API）：
   ```bash
   .venv/bin/pip install sionna
   ```
   - 若仍遇到 `cryptography` 编译问题，同样可尝试固定版本（如 `cryptography==41.0.7`，需确认该版本是否有 py3.11 wheel，若无则找一个有 wheel 的相近版本）。
4. 验证安装：
   ```bash
   DRJIT_LIBLLVM_PATH=/usr/local/Cellar/llvm/22.1.8/lib/libLLVM.dylib \
     .venv/bin/python -c "import sionna; print(sionna.__version__); from sionna.rt import Scene, dbm_to_watt, ITURadioMaterial; print('OK')"
   ```
5. 若验证通过，可考虑将 `DRJIT_LIBLLVM_PATH` 写入 `.venv` 的 activate 脚本或项目 `.env`，避免每次手动 export。
6. （可选）检查 `Lib_xhd/tools/sionna_xhd/` 下的代码是否与新版 `sionna` 1.x API 完全兼容（笔记本导入的 `ITURadioMaterial`、`scene as sc`、`dr.cuda.Float` 等均为新 API 特征，需要重点验证）。

## 关键环境信息（供快速恢复上下文）
| 项目 | 值 |
|---|---|
| Workspace 根目录 | `/Users/alonekite/CascadeProjects/Study_Sionna_BX` |
| 系统 Python | `/usr/bin/python3` (3.9.6，未使用) |
| Homebrew Python | `/usr/local/bin/python3.11` (3.11.15，**下次用这个建 venv**) |
| Homebrew | 已安装于 `/usr/local` |
| LLVM | `/usr/local/Cellar/llvm/22.1.8/`，共享库路径：`/usr/local/Cellar/llvm/22.1.8/lib/libLLVM.dylib` |
| 需要的环境变量 | `DRJIT_LIBLLVM_PATH=/usr/local/Cellar/llvm/22.1.8/lib/libLLVM.dylib` |
| 旧 `.venv`（待删除） | `/Users/alonekite/CascadeProjects/Study_Sionna_BX/.venv`（基于 py3.9, sionna==0.19.2） |

## 下次对话可直接说
> "继续安装" 或 "按 handoff20260809.md 继续"
