# Handoff 2026-08-10

## 背景
延续 `handoff20260809.md` 的工作，在 `mMIMO_sleep` 项目下（区别于 `Study_Sionna_BX`）完成本地开发环境（Mac，Intel x86_64）的 venv 搭建，并明确了 Mac 本地与 RunPod（RTX 4090）之间的职责划分。

## 环境职责划分（重要约定）
- **Mac（本机，Intel x86_64，macOS 12.7.6 Monterey）**：仅用于 CPU 上的 Python 开发——codebook / muting pattern 验证、单元测试、Notebook 编写与调试、小规模 PyTorch 调试。**不安装、不编译 Sionna RT / Mitsuba / Dr.Jit，不涉及 CUDA。**
- **RunPod（RTX 4090，Linux + CUDA）**：负责 Sionna RT 射线追踪、Mitsuba/Dr.Jit、大规模信道数据生成、GPU 训练。这里才安装 `rt` 依赖组。

## 本次完成的工作

### 1. 创建 Mac 本地 venv
- 使用 Homebrew 安装的 `/usr/local/bin/python3.11`（3.11.15，满足 `pyproject.toml` 的 `requires-python = ">=3.11,<3.12"`）在 `mMIMO_sleep` 项目根目录下创建 `.venv`：
  ```bash
  /usr/local/bin/python3.11 -m venv .venv
  .venv/bin/pip install --upgrade pip
  ```

### 2. 安装依赖（仅 dev，不装 rt）
```bash
.venv/bin/pip install -e ".[dev]"
```
- 明确**未**执行 `pip install -e ".[rt]"`，也**未**尝试从源码编译 Mitsuba。
- 原因：`sionna-rt==1.2.2` 依赖 `mitsuba==3.8.0`，而该版本在 PyPI 上**没有 macOS x86_64（Intel）的预编译 wheel**（只有 `macosx_11_0_arm64`），只能源码编译，且本机也不需要跑 RT，因此按约定跳过。

### 3. 修复 NumPy / PyTorch ABI 不兼容问题（根因修复，已保留）
- `pyproject.toml` 原依赖为 `numpy>=1.26,<3`，导致 pip 解析到 `numpy 2.4.6`，与 `torch==2.2.2`（基于 NumPy 1.x ABI 编译）不兼容，import 时报 `_ARRAY_API not found` 警告并有崩溃风险。
- 已将 `pyproject.toml` 中的约束改为：
  ```toml
  "numpy>=1.26,<2",
  ```
- 并在 venv 中执行 `pip install "numpy<2"` 降级到 `numpy==1.26.4`，验证 `torch`/`numpy`/`matplotlib` 正常导入，无警告。

### 4. 验证
```bash
.venv/bin/python -c "import torch, numpy, matplotlib, pytest; print(torch.__version__, numpy.__version__)"
.venv/bin/pytest -q
```
- torch 2.2.2 + numpy 1.26.4 正常协同工作。
- `pytest` 可正常收集运行（当前 `tests/` 目录下暂无用例，`testpaths = ["tests"]` 在 `pyproject.toml` 中已配置）。

### 5. 注册 Jupyter Kernel（用户在会话后自行执行）
```bash
./.venv/bin/python -m pip install ipykernel   # 已安装，命令确认无变化
./.venv/bin/python -m ipykernel install --user --name mmimo-sleep --display-name "mMIMO_sleep (.venv)"
```
- Kernel 已注册到 `/Users/alonekite/Library/Jupyter/kernels/mmimo-sleep`，可在 `notebook/validate_pmi_muting_patterns.ipynb` 等 notebook 中选择 "mMIMO_sleep (.venv)" 内核运行。

### 6. 推送代码到 GitHub
- 初始化本地 Git 仓库并创建 `.gitignore`（排除 `.venv/`、`__pycache__/`、`.pytest_cache/`、`*egg-info/` 等）。
- 提交初始 commit（`4de8379`），包含源码、`pyproject.toml`、notebook 与 handoff 文件。
- 使用 `gh repo create mMIMO_sleep --public --source=. --remote=origin --push` 创建公开仓库并推送：
  - 仓库地址：`https://github.com/alonekite/mMIMO_sleep`
  - 本地 `main` 已跟踪 `origin/main`

## 当前环境状态速查
| 项目 | 值 |
|---|---|
| 项目根目录 | `/Users/alonekite/CascadeProjects/mMIMO_sleep` |
| venv 路径 | `/Users/alonekite/CascadeProjects/mMIMO_sleep/.venv` |
| venv Python | 3.11.15（来自 `/usr/local/bin/python3.11`） |
| 已安装依赖组 | `dev`（含 torch 2.2.2, numpy 1.26.4, matplotlib, pytest, jupyterlab, ipykernel, ruff 等） |
| 未安装依赖组 | `rt`（sionna-rt==1.2.2，含 mitsuba/dr.jit，留给 RunPod） |
| Jupyter Kernel | `mmimo-sleep`，显示名 "mMIMO_sleep (.venv)" |
| `pyproject.toml` numpy 约束 | `numpy>=1.26,<2`（已从 `<3` 收紧，修复 torch ABI 不兼容） |
| GitHub 仓库 | `https://github.com/alonekite/mMIMO_sleep`（公开，已推送 `main` 分支） |

## 下一步 / 待办
1. **RunPod 端搭建**（尚未开始，下次在 RunPod 上执行）：
   ```bash
   python3.11 -m venv .venv
   .venv/bin/pip install --upgrade pip
   .venv/bin/pip install -e ".[dev,rt]"
   ```
   预期在 Linux x86_64 + CUDA 环境下，`mitsuba==3.8.0` 有 `manylinux_2_28_x86_64` wheel，可直接 pip 安装，无需编译；同时需确认 GPU 驱动、CUDA 版本与 Dr.Jit CUDA 后端的兼容性。
2. 补充 `tests/` 目录下的单元测试用例（目前 `pyproject.toml` 已配置 `testpaths = ["tests"]`，但目录下暂无测试文件）。
3. 继续在 `notebook/validate_pmi_muting_patterns.ipynb` 中做 codebook（参考 `src/mMIMO_sleep/codebook/dft.py`）与 muting pattern 的验证工作。
4. （可选）在 RunPod 上完成 RT 环境验证后，检查 notebook 中依赖新版 `sionna` 1.x API（如 `ITURadioMaterial`）的部分是否兼容，参考 `handoff20260809.md` 中记录的 API 差异问题。

## 下次对话可直接说
> "继续按 handoff20260810.md 的计划，在 RunPod 上搭建 rt 环境" 或 "继续 codebook/muting 验证工作"
